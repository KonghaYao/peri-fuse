{
  "name": "langfuse-lite",
  "version": "0.1.0",
  "description": "Standalone lightweight Langfuse server — SQLite-backed, no Next.js, no ClickHouse, no Redis",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm --filter @langfuse-lite/shared run build && pnpm --filter @langfuse-lite/server run build && pnpm --filter @langfuse-lite/web run build",
    "dev": "concurrently \"pnpm --filter @langfuse-lite/server run dev\" \"pnpm --filter @langfuse-lite/web run dev\"",
    "dev:server": "pnpm --filter @langfuse-lite/server run dev",
    "dev:web": "pnpm --filter @langfuse-lite/web run dev",
    "start": "pnpm --filter @langfuse-lite/server run start",
    "typecheck": "pnpm -r run typecheck",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "pnpm -r run test",
    "db:generate": "pnpm --filter @langfuse-lite/shared run db:generate",
    "db:push": "pnpm --filter @langfuse-lite/shared run db:push"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "better-sqlite3",
      "esbuild",
      "prisma",
      "protobufjs"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.0",
    "concurrently": "^9.1.0"
  }
}
